SELECT
  *
FROM
  Country_language
WHERE
  Language LIKE '%{{Table1.searchText}}%'
ORDER BY
  {{Table1.sortOrder.column || 'Language'}} {{Table1.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{Table1.pageSize}}
OFFSET
  {{Table1.pageOffset}}